---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "biznetgio_neolite_vm_from_snapshot Resource - biznetgio"
subcategory: ""
description: |-
  VM NEO Lite baru yang di-restore dari snapshot (endpoint restore-with). Hasilnya VM biasa: delete resource = delete VM. Semua input create-only.
---

# biznetgio_neolite_vm_from_snapshot (Resource)

VM NEO Lite baru yang di-restore dari snapshot (endpoint restore-with). Hasilnya VM biasa: delete resource = delete VM. Semua input create-only.

## Example Usage

```terraform
data "biznetgio_neolite_products" "all" {}

resource "biznetgio_neolite_keypair" "main" {
  name = "neo-lite-key"
}

resource "biznetgio_neolite_vm" "source" {
  ssh_and_console_user = "admin"
  console_password     = "s3cretP4ssw0rd"
  vm_name              = "neo-lite-source"
  product_id           = data.biznetgio_neolite_products.all.products[0].product_id
  select_os            = "ubuntu-22"
  keypair_id           = biznetgio_neolite_keypair.main.keypair_id
  cycle                = "m"
}

resource "biznetgio_neolite_snapshot" "main" {
  neolite_account_id = biznetgio_neolite_vm.source.id
  name               = "pre-update"
  cycle              = "m"
}

# VM baru yang di-restore dari snapshot. Delete resource = delete VM.
resource "biznetgio_neolite_vm_from_snapshot" "restored" {
  snapshot_id          = biznetgio_neolite_snapshot.main.id
  product_id           = data.biznetgio_neolite_products.all.products[0].product_id
  cycle                = "m"
  keypair_id           = biznetgio_neolite_keypair.main.keypair_id
  name                 = "neo-lite-restored"
  ssh_and_console_user = "admin"
  console_password     = "s3cretP4ssw0rd"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `console_password` (String, Sensitive) Password console saat create. Write-only: ga pernah di-refetch dari API.
- `cycle` (String) Siklus billing: `m` monthly, `a` annual, `q` quarterly, `s` semiannual, `b` biennial, `t` triennial, `p4`, `p5`.
- `keypair_id` (Number) Id keypair dari `biznetgio_neolite_keypair`.
- `name` (String) Nama VM hasil restore. Panjang 6-16, hanya huruf/angka/titik/dash.
- `product_id` (Number) Product id dari data source `biznetgio_neolite_products` atau portal.
- `snapshot_id` (Number) Account id snapshot sumber, dari `biznetgio_neolite_snapshot`.
- `ssh_and_console_user` (String) User SSH & console yang dipasang saat create.

### Optional

- `description` (String) Deskripsi VM.
- `pay_with_credit_card` (Boolean) Bayar invoice pake kartu kredit saat order. Default true (auto-charge). Set false kalau mau ninggalin invoice unpaid di portal — resource bakal stuck Pending sampai dibayar.
- `promocode` (String) Kode promo saat order.
- `timeouts` (Attributes) (see [below for nested schema](#nestedatt--timeouts))

### Read-Only

- `id` (String) Resource id = account_id VM hasil restore di BiznetGIO.
- `order_id` (String) Order id dari response create.
- `status` (String) Status VM (Active, Pending, Suspended, Terminated).

<a id="nestedatt--timeouts"></a>
### Nested Schema for `timeouts`

Optional:

- `create` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours).
- `delete` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours). Setting a timeout for a Delete operation is only applicable if changes are saved into state before the destroy operation occurs.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
terraform import biznetgio_neolite_vm_from_snapshot.restored <account_id>
```
