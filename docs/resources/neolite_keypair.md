---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "biznetgio_neolite_keypair Resource - biznetgio"
subcategory: ""
description: |-
  SSH keypair untuk NEO Lite (pool keypair neolite, terpisah dari baremetal/gpu). Keypair di-generate server; private key cuma keluar sekali di response create.
---

# biznetgio_neolite_keypair (Resource)

SSH keypair untuk NEO Lite (pool keypair neolite, terpisah dari baremetal/gpu). Keypair di-generate server; private key cuma keluar sekali di response create.

## Example Usage

```terraform
# Keypair di-generate server; private key cuma keluar sekali di response create.
resource "biznetgio_neolite_keypair" "main" {
  name = "neo-lite-key"
}

# Import keypair yang sudah ada:
# resource "biznetgio_neolite_keypair" "imported" {
#   name = "existing-key"
# }
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) Nama keypair.

### Read-Only

- `id` (String) Resource id = keypair_id.
- `keypair_id` (Number) Id keypair di BiznetGIO.
- `private_key` (String, Sensitive) Private key (sensitive). Write-only: cuma ada di response create, ga bisa di-refetch.
- `public_key` (String) Public key yang di-generate.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
terraform import biznetgio_neolite_keypair.main <keypair_id>
```
