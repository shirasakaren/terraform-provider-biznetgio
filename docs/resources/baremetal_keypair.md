---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "biznetgio_baremetal_keypair Resource - biznetgio"
subcategory: ""
description: |-
  SSH keypair untuk NEO Metal (pool keypair baremetal, terpisah dari neolite/gpu). Keypair di-generate server; private key cuma keluar sekali di response create.
---

# biznetgio_baremetal_keypair (Resource)

SSH keypair untuk NEO Metal (pool keypair baremetal, terpisah dari neolite/gpu). Keypair di-generate server; private key cuma keluar sekali di response create.

## Example Usage

```terraform
# Keypair di-generate server; private key cuma keluar sekali di response create.
resource "biznetgio_baremetal_keypair" "main" {
  name = "neo-metal-key"
}

# Import keypair yang sudah ada:
# resource "biznetgio_baremetal_keypair" "imported" {
#   name       = "existing-key"
#   public_key = "ssh-rsa AAAA..."
# }
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) Nama keypair.

### Optional

- `public_key` (String) Kalau diisi, keypair di-import via `POST /baremetals/keypairs/import`. Kalau kosong, keypair di-generate server. Output-nya selalu public key dari API.

### Read-Only

- `id` (String) Resource id = keypair_id.
- `keypair_id` (Number) Id keypair di BiznetGIO.
- `private_key` (String, Sensitive) Private key dari response create (kalau API ngasih). Cuma muncul sekali; di Read berikutnya di-keep dari state.
- `raw` (String, Sensitive) Full JSON item keypair dari list API.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
terraform import biznetgio_baremetal_keypair.main <keypair_id>
```
