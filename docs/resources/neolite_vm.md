---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "biznetgio_neolite_vm Resource - biznetgio"
subcategory: ""
description: |-
  NEO Lite virtual machine. Order VM, update nama/keypair/package/storage/power, rebuild OS, atau migrate ke NEO Lite Pro.
---

# biznetgio_neolite_vm (Resource)

NEO Lite virtual machine. Order VM, update nama/keypair/package/storage/power, rebuild OS, atau migrate ke NEO Lite Pro.

## Example Usage

```terraform
data "biznetgio_neolite_products" "all" {}

data "biznetgio_neolite_os_list" "ubuntu" {
  product_id = data.biznetgio_neolite_products.all.products[0].product_id
}

resource "biznetgio_neolite_keypair" "main" {
  name = "neo-lite-key"
}

# Order VM, tunggu sampai status Active.
resource "biznetgio_neolite_vm" "main" {
  ssh_and_console_user = "admin"
  console_password     = "s3cretP4ssw0rd"
  vm_name              = "neo-lite-1"
  product_id           = data.biznetgio_neolite_products.all.products[0].product_id
  select_os            = data.biznetgio_neolite_os_list.ubuntu.oss[0].name
  keypair_id           = biznetgio_neolite_keypair.main.keypair_id
  cycle                = "m"
}

# Upgrade disk (absolute target GB — cuma bisa naik):
# disk_size = 100

# Ganti power: power_state = "stop" (start/stop/suspend/resume/shutdown)

# Rebuild OS: rebuild_os = data.biznetgio_neolite_os_list.ubuntu.oss[1].name

# Migrate ke NEO Lite Pro: migrate_to_pro = "12345"  # neolitepro_product_id
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `console_password` (String, Sensitive) Password console saat create. Write-only: ga pernah di-refetch dari API. Minimal 8 karakter alphanumeric, harus ada huruf kecil, huruf besar, dan angka.
- `cycle` (String) Siklus billing: `m` monthly, `a` annual, `q` quarterly, `s` semiannual, `b` biennial, `t` triennial, `p4`, `p5`.
- `keypair_id` (Number) Id keypair dari `biznetgio_neolite_keypair`. Bisa diganti via change-keypair.
- `product_id` (Number) Product id dari data source `biznetgio_neolite_products` atau portal.
- `select_os` (String) OS yang dipasang saat create, dari data source `biznetgio_neolite_os_list`. Ganti OS = pakai `rebuild_os`.
- `ssh_and_console_user` (String) User SSH & console yang dipasang saat create. Panjang 6-32, hanya huruf/angka/titik/dash.

### Optional

- `description` (String) Deskripsi VM. Create-only: ga ada endpoint update, jadi nilai dari API yang dipakai.
- `disk_size` (Number) Ukuran disk target (GB, absolute — bukan tambahan). Cuma bisa naik, bukan turun.
- `migrate_to_pro` (String) Trigger one-shot migrate ke NEO Lite Pro: isi `neolitepro_product_id` target (lihat data source `biznetgio_neolite_change_package_options` family). Ganti nilainya buat re-trigger.
- `pay_with_credit_card` (Boolean) Bayar invoice pake kartu kredit saat order. Default true (auto-charge). Set false kalau mau ninggalin invoice unpaid di portal — resource bakal stuck Pending sampai dibayar. Bisa diubah in-place via change-package/storage.
- `power_state` (String) Power state VM: `start`, `stop`, `suspend`, `resume`, atau `shutdown`. Update cuma mengirim action kalau nilainya berubah.
- `promocode` (String) Kode promo saat order.
- `rebuild_os` (String) Kalau berubah, VM di-rebuild (wipe OS) pake OS baru via endpoint rebuild. List OS valid ada di data source `biznetgio_neolite_os_list`.
- `timeouts` (Attributes) (see [below for nested schema](#nestedatt--timeouts))
- `vm_name` (String) Nama VM. Default `server-name`. Bisa diubah via change-vm-name. Panjang 6-16, hanya huruf/angka/titik/dash.

### Read-Only

- `billingcycle` (String) Siklus billing aktif.
- `cipassword` (String, Sensitive) Cloud-init password VM (sensitive).
- `ciuser` (String) Cloud-init user VM.
- `cpus` (Number) Jumlah CPU VM.
- `id` (String) Resource id = account_id dari BiznetGIO.
- `last_invoice` (Attributes) Invoice terakhir VM. (see [below for nested schema](#nestedatt--last_invoice))
- `maxdisk` (Number) Ukuran disk maksimal VM (GB).
- `maxmem` (Number) Memory maksimal VM (MB).
- `mem` (Number) Memory yang dipakai VM (MB).
- `next_due` (String) Tanggal tagihan berikutnya.
- `order_id` (String) Order id dari response create.
- `osname` (String) Nama OS yang jalan di VM.
- `product_name` (String) Nama product aktif.
- `raw` (String, Sensitive) Full JSON response akun terakhir dari API, buat akses field yang belum dimodel (cipassword di-mask).
- `recurring_amount` (Number) Nominal recurring per siklus.
- `region` (String) Region VM.
- `region_label` (String) Label region VM.
- `status` (String) Status akun terakhir dari API (Active, Pending, Suspended, Terminated).
- `uptime` (Number) Uptime VM dalam detik.

<a id="nestedatt--timeouts"></a>
### Nested Schema for `timeouts`

Optional:

- `create` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours).
- `delete` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours). Setting a timeout for a Delete operation is only applicable if changes are saved into state before the destroy operation occurs.
- `update` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours).


<a id="nestedatt--last_invoice"></a>
### Nested Schema for `last_invoice`

Read-Only:

- `date` (String) Tanggal invoice.
- `datepaid` (String) Tanggal dibayar.
- `duedate` (String) Tanggal jatuh tempo.
- `id` (Number) Id invoice.
- `invoice_type` (String) Tipe invoice.
- `paid_id` (Number) Id pembayaran invoice.
- `paybefore` (String) Batas bayar.
- `status` (String) Status invoice.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
terraform import biznetgio_neolite_vm.main <account_id>
```
