---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "biznetgio_object_storage_credential Resource - biznetgio"
subcategory: ""
description: |-
  S3 credential (access/secret key pair) for an Object Storage instance. The secret key is shown only once at create time and cannot be re-fetched. Import with terraform import biznetgio_object_storage_credential.example <account_id>:<access_key>; the id is then normalized to the hashed form <account_id>:<sha256(access_key)[:16]>.
---

# biznetgio_object_storage_credential (Resource)

S3 credential (access/secret key pair) for an Object Storage instance. The secret key is shown only once at create time and cannot be re-fetched. Import with `terraform import biznetgio_object_storage_credential.example <account_id>:<access_key>`; the id is then normalized to the hashed form `<account_id>:<sha256(access_key)[:16]>`.

## Example Usage

```terraform
# Secret key is shown only once at create; afterwards it keeps its state value.
resource "biznetgio_object_storage_credential" "example" {
  account_id = biznetgio_object_storage.example.id
  active     = true
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `account_id` (String) Object Storage instance account id.

### Optional

- `active` (Boolean) Whether the credential is enabled. Set false to disable without deleting.

### Read-Only

- `access_key` (String, Sensitive) Credential access key, returned once at create.
- `id` (String) Composite id `<account_id>:<sha256 hex dari access key, 16 char pertama>` - access key plaintext ga pernah masuk id.
- `raw` (String, Sensitive) Full JSON of the last read from the API, for anything not modeled yet.
- `secret_key` (String, Sensitive) Credential secret key, shown only once at create; keeps its last state value on refresh.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
terraform import biznetgio_object_storage_credential.example <account_id>:<access_key>
```
